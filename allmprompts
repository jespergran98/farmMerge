Using the attached `implementationGuide.md` and `README.md`, build a complete Farm Merge Valley sticker season simulator as a single `index.html` file with embedded or linked `styles.css` and `simulator.js`. Follow the implementation guide exactly — every mechanic, probability, pack content table, sticker count, star cost, duplicate reward, and vault rule must come exclusively from the README with no invented values. Implement the full `SimulatorEngine` class with seeded PRNG (mulberry32 or xoshiro128**), all five pack types, vault pack logic (guaranteed non-duplicate from uncollected pool, remainder slots use normal odds), the 5-day login cycle, all four challenge settings, and the full daily loop order (login → challenges → vault). The vault engine must respect all five config parameters: enabled tiers, purchase priority order, start/end day, minimum star reserve, and max purchases per day. The four preset buttons must instantly populate all vault fields, and the Strategy Comparison section must re-run all four presets synchronously using the same seed.

The UI must fill exactly 100svh in the top section (no scroll, no overflow) with the left control panel and right 108-card sticker album side by side, followed by a scrollable stats section below containing all seven subsections: Season Summary, Daily Timeline, Cumulative Progress Chart (SVG/Canvas, dual-line animated), Pack Yield Breakdown, Rarity Heatmap with hover tooltips, Star Economy Waterfall (fully reconciled — inflows minus outflows must equal remaining stars), and Strategy Comparison table. Apply the warm farm aesthetic throughout: wheat gold, barn red, sage green, cream, and soil brown palette; Fredoka One for headings; Nunito for body; CSS SVG grain texture; spring animations at 1×–10× speed; zero animations at Max speed. Build all charts by hand using SVG or Canvas — no charting libraries.

The simulation should be quick and give detailed information of the average sticker count at the end of each simulated season, if the user applied day settings to when stars were spent on sticker packs, save these settings in stats, so the user can compare the final sticker count at the end of a season depending on which days the sticker packs were opened. When the fast speed is activated for simulation speed, it should run at the fastest time possible, at runtime. The logic must be clean, flawless and perfect.

Provide clean, streamlined, refactored and perfect code following 2026 practices. Provide the code before running out of token limit. The index.html is already complete, provide the simulator.js and styles.css files.