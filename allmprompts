**Prompt for `index.html`:**

Using the attached `implementationGuide.md` and `README.md`, build the complete `index.html` file for the Farm Merge Valley sticker season simulator. This file is the structural foundation — it defines the full DOM layout that engine.js, ui.js, and stats.js and styles.css will bind to and style. Include `<link>` tags for Google Fonts (Fredoka One, Nunito) and `styles.css`, and a `<script>` tag for js files at the bottom of `<body>`. The layout has two major regions: a top section that fills exactly `100svh` containing the left control panel and right sticker album side by side, and a scrollable stats section below. The control panel must contain all simulation inputs — seed field, simulation speed selector, days-to-simulate input, the 5-day login cycle toggle, daily challenge difficulty selector with all four task-count presets, and the full vault configuration block (enabled tiers checkboxes, purchase priority order, start/end day fields, minimum star reserve, max purchases per day, and four named strategy preset buttons). The sticker album must render all 108 sticker card slots grouped by rarity tier (27 × 1-star, 21 × 2-star, 18 × 3-star, 18 × 4-star, 24 × 5-star) with stable `data-sticker-id` and `data-rarity` attributes so JS can toggle a collected state. The stats section must contain exactly seven subsections in order: Season Summary, Daily Timeline, Cumulative Progress Chart (an `<svg>` or `<canvas>` container), Pack Yield Breakdown, Rarity Heatmap (grid of cells with `data` attributes for JS to populate), Star Economy Waterfall (an `<svg>` or `<canvas>` container), and Strategy Comparison table with a `<tbody>` JS can rewrite. Every interactive element must have a clear `id` or `data-*` attribute matching what the implementation guide specifies — no IDs or classes may be invented. Provide clean, semantic HTML5 with zero inline styles and zero inline scripts.

---

**Prompt for `engine.js`:**

Using the attached `implementationGuide.md`, `README.md` and `index.html`, build the complete `engine.js` file for the Farm Merge Valley sticker season simulator. This file contains all game logic and zero DOM interaction.

Implement the `mulberry32` PRNG exactly as specified — every random draw (rarity rolls and sticker ID selection) must flow through it in a fixed, deterministic order. No `Math.random()` anywhere. Export the `SimulatorEngine` class with `constructor({ seed, challengeSetting, vaultConfig })`, `reset()`, `runDay()`, and `runAll()` methods, plus `getState()`. Export the module-level `seasonHistory` mutable array and `clearSeasonHistory()` function.

Implement all five data structures exactly as specified: `SimulatorState`, `DayResult`, `SeasonResult`, `PackResult`, and `VaultPurchase`, with every field name and type matching the guide. Implement the `rarityOf(id)` helper using the exact ID ranges (1–27 = 1★, 28–48 = 2★, 49–66 = 3★, 67–84 = 4★, 85–108 = 5★). Implement `reset()` to restore all 108 sticker drawCounts to 0, stars to 0, uniqueCount to 0, log to `[]`, day to 0, and re-seed the PRNG. `runDay()` must throw if called when `this.day === 50`.

Implement the daily execution order exactly: (1) open login pack using `((day - 1) % 5) + 1` tier, (2) open challenge packs per the configured setting, (3) run the vault purchase loop with the restart-from-top rule. For regular pack opening: guaranteed slot draws uniformly from all stickers of that rarity; each additional slot uses the exact probability tables from `README.md` then draws uniformly from all stickers of the rolled rarity. For vault packs: the guaranteed slot draws only from uncollected stickers of that rarity and never awards duplicate stars; remainder slots use normal additional-slot probabilities. All five vault pre-purchase conditions must be checked. After every completed season (via `runAll()` or the final `runDay()`), append a `SeasonRecord` to `seasonHistory`. After every completed season, synchronously run all five strategy presets on the same seed and challenge setting and store the results in a `currentSeasonComparison` export. The auto-play seeding rule is season N uses `baseSeed + (N - 1)`.

---

**Prompt for `ui.js`:**

Using the attached `implementationGuide.md`, `README.md`, `index.html`, and `engine.js`, build the complete `ui.js` file for the Farm Merge Valley sticker season simulator. This is the only module that touches the DOM directly during simulation. Import `SimulatorEngine`, `seasonHistory`, and `clearSeasonHistory` from `engine.js`; import `renderStats` from `stats.js`.

Implement the speed system: five speeds (1×/500ms, 2×/500ms, 5×/200ms, 10×/100ms, Max/0ms). At 1×–10×, use `setTimeout(runNextDay, currentDelay)` between `runDay()` calls; speed changes take effect on the next pending timeout. At Max speed, call `engine.runAll()` synchronously and flush the entire DOM in one repaint with no animations. Every speed change must update `--anim-dur` on `:root` and set `--shimmer-state` to `paused` at Max, `running` otherwise`.

Implement the sticker album renderer: 108 cards grouped by rarity, each bound by `data-sticker-id` and `data-rarity`. Uncollected cards show `?` with `#D8CDB8` background. On new collection (non-Max): add the `pop-in` class to trigger the spring scale animation and remove it after `--anim-dur`. On duplicate (non-Max): add the `dupe-flash` class and remove it after `calc(--anim-dur * 0.5)`. At Max speed apply all state changes in a single DOM flush with no class toggling. Keep rarity section headers updated with live counts (`12 / 27`). Keep the full-width progress bar updated with `X / 108 stickers collected (X%)`.

Implement the digit-slot star counter animation: each digit rendered in a vertically clipped container, sliding upward on increase and downward on decrease, duration from `--anim-dur`. Implement the live event log: show the 8 most recent `EventEntry` items, new entries slide in from the bottom pushing older ones up, format exactly as shown in the guide (`Day 5  5★ Login  →  ★5 ★3 ...`). At Max speed render only the final 8 entries with no animation.

Implement the auto-play toggle: amber pulsing outline on the Run/Pause group, `AUTO` pill badge visible. Between auto-play seasons: append `SeasonRecord` to `seasonHistory`, call `renderStats`, wait 1 000 ms (fixed), then reset using `baseSeed + (nextSeasonNumber - 1)` and begin the next season. Bind all vault preset buttons so clicking one updates every editable field atomically and marks the active preset. Changing any individual field switches the active label to `Custom`. Seed edits trigger a reset. Challenge setting changes take effect from the next day.

---

**Prompt for `stats.js`:**

Using the attached `implementationGuide.md`, `README.md`, `index.html`, `engine.js`, and `ui.js`, build the complete `stats.js` file for the Farm Merge Valley sticker season simulator. This module is a pure renderer — it reads data and writes DOM, nothing else. It imports nothing from `engine.js` directly. Export a single function `renderStats(seasonResult, seasonHistory, vaultConfig)`.

Implement the Season History layer first. Render it only after at least one season has completed; before that the layer is empty. The Season Outcome Chart is an SVG/Canvas with two vertically stacked panels sharing an X-axis: the top panel shows unique stickers collected per season as vertical bars coloured by preset (Spend Greedily = `--color-green`, 5★ Only = `--color-gold`, Hoard Then Spend = `--color-sky`, End-Season Blitz = `#E67E22`, Never Spend = `#9E9E9E`, Custom = `--color-brown`) with a dashed reference line at 108 and a dashed line at the running average; the bottom panel shows a stacked bar of stars spent (barn red) + stars remaining (wheat gold) per season. Hovering any bar shows a tooltip with season number, preset name, stickers collected, stars spent, and stars remaining. Below the chart render the Season History Table (one row per season, newest first, left-border colour-coded by preset) and the Strategy Averages Block (one card per distinct preset seen in history, showing seasons run, average stickers, completion rate, average stars remaining). Render a `Clear History` button that calls `clearSeasonHistory()` and collapses this layer.

Implement the Current Season layer with exactly seven subsections in this order. (1) Season Summary: a stat-card grid with every field from the guide. (2) Daily Timeline: a horizontally scrollable strip of 50 columns, each showing day number, login tier, challenge packs, vault purchases, net new uniques in green, stars earned, stars spent in red, with an amber top border on the vault start day and pale red background on days where star balance hit zero. (3) Cumulative Progress Chart: SVG dual-axis line chart (left = stickers in `--color-green`, right = stars in `--color-gold`), both lines animate drawing left-to-right over 800 ms (fixed, not tied to sim speed), vertical dashed amber line at vault start day (omit if no tiers enabled), grid lines every 10 stickers and every 500 stars, right Y-axis max = `Math.max(500, Math.ceil(peakStarBalance / 500) * 500)`. (4) Pack Yield Breakdown: for each pack type opened, two horizontal stacked bars (actual vs expected) with per-segment percentage labels and rarity colour legend; vault packs tracked separately. (5) Rarity Heatmap: five rows matching rarity counts (27/21/18/18/24), cell fill `#BDBDBD` if uncollected or rarity accent colour at opacity `1.0 − (collectionDay − 1) / 49 × 0.65` if collected, hover tooltip showing sticker ID, rarity, collection day, and total draw count. (6) Star Economy Waterfall: a waterfall bar chart with inflows (upward, `--color-green`) broken down by source and rarity, outflows (downward, `--color-red`) per vault tier, final balance bar in wheat gold, numeric labels on every segment, and a summary row verifying `earned − spent = remaining`. (7) Strategy Comparison table: 5 or 6 rows in fixed preset order using the `currentSeasonComparison` data from `engine.js`, highlight the best-unique-count row in `--color-green`, include an inline proportional bar per row, and a one-sentence annotation below each row exactly as specified in the guide.

---

**Prompt for `styles.css`:**

Using the attached `implementationGuide.md`, `README.md`, and `index.html`, build the complete `styles.css` file for the Farm Merge Valley sticker season simulator. This file provides all visual styling, CSS variables, and keyframe animations. No inline styles exist in the project — every visual rule lives here.

Define all eight CSS variables on `:root` exactly as specified: `--color-gold: #F5C842`, `--color-red: #C0392B`, `--color-orange: #E67E22`, `--color-green: #7DAA72`, `--color-cream: #FDF6E3`, `--color-brown: #6B4423`, `--color-sky: #5B9BD5`, `--color-purple: #8E6BBF`. Also define `--anim-dur: 400ms` and `--shimmer-state: running` as defaults on `:root` — `ui.js` will override these at runtime. Apply Google Fonts: `Fredoka One` for headings, card labels, badge numbers, preset button labels, day counter, and star counter; `Nunito` for body text, event log, stat values, table content, and tooltips.

Style the top section as an exactly `100svh` two-column layout (left panel ~38%, right panel ~62%) with no overflow and no scroll. Style the bottom stats section as a scrollable region below the viewport edge. Apply the panel card style (`border-radius: 8px`, `border: 2.5px solid var(--color-brown)`, `background: var(--color-cream)`) to all major containers. Apply the grain texture using `.panel::before` with `position: absolute; inset: 0; filter: url(#grain); opacity: 0.07; pointer-events: none; border-radius: inherit` — panels must use `position: relative` and `overflow: hidden`.

Implement all four `@keyframes` animations. Card pop-in (`pop-in`): `0% { transform: scale(1) } 50% { transform: scale(1.3) } 100% { transform: scale(1) }` with `cubic-bezier(0.34, 1.56, 0.64, 1)`, duration from `var(--anim-dur)`. Shimmer sweep on collected cards: animate `background-position` from `0% 0%` to `200% 200%` on a 45° `linear-gradient` with `background-size: 300% 300%`, 3 s infinite, `animation-play-state: var(--shimmer-state)`. Duplicate flash (`.dupe-flash`): CSS `transition` on `background-color` to `var(--color-gold)` and back, duration `calc(var(--anim-dur) * 0.5)`. Auto-play pulse: amber `box-shadow` keyframe on the active button group, 1.2 s infinite, paused when auto-play is off.

Style uncollected sticker cards with `background: #D8CDB8; filter: grayscale(0.5) opacity(0.7)` and a large centred `?`. Style collected cards with a rarity-tinted background, star badge in the corner, and a persistent `box-shadow: 0 0 10px 3px <rarity-color>55` glow — using distinct colours for each of the five rarities matching the rarity header colours (silver-grey for 1★, leaf green for 2★, `--color-sky` for 3★, `--color-purple` for 4★, `--color-gold` for 5★). Use `clamp()` on card sizing so all 108 cards fit the right panel without overflow at 1080p. At `--anim-dur: 0ms`, ensure `transition: none` and `animation: none` are applied globally so the Max speed DOM flush is instantaneous.

---

Im developing a farm merge valley simulator meant to showcase the probability of collecting all stickers throughout a 50 day season depending on when the user buys the sticker packs using stars. Analyze the full application. Whats the result when buying stickerpacks as soon as you can afford them? What if you wait until the final moments before the season ends? What happens if you only open the best stickerpacks when you can afford them, or what happens if you spend all your stars on 5 star packs, and any remaining stars on other packs, just as the season ends? All of these scenarios need to be accounted for in the implementation guide, ensuring the user gets proper statistics. There should be an auto play mode that restarts seasons as soon as they end. The 1x speed should be 1 day takes a second, at 10x speed, the simulation should take 0.1 sec per day, so the entire simulation takes 5 seconds. All animations also need to account for this. At the max speed toggle, the simulation should perform at max performance at run time. The project should have a single index.html file, and several js and css files if required if this makes the project better. The goal is to have a visually perfect farm merge valley simulator in farm merge valley theme, perfectly simulating the numbers in the README.md file. Analyze the implementationGuide.md carefully and ensure all requirements I've listed is present in that file. When I create the website, it will use the implementationGuide.md as a reference alongside the README.md. Ensure the implementationGuide is crystal clear and all requirements I've asked for is specified in the file. Analyze the implementationGuide carefully and remove any design or logic you dont like

---

Make this text prompt crystal clear and perfect for AI:

Im developing a farm merge valley simulator meant to showcase the probability of collecting all stickers throughout a 50 day season depending on when the user buys the sticker packs using stars. The "vault strategy" section is really confusing and need major improvements.

Analyze the full application. Whats the result when buying stickerpacks as soon as you can afford them? What if you wait until the final moments before the season ends? What happens if you only open the best stickerpacks when you can afford them, or what happens if you spend all your stars on 5 star packs, and any remaining stars on other packs, just as the season ends? All of these scenarios need to be accounted for and easy to understand in the UI. Ensure the user gets proper statistics.

The goal is to have a visually perfect farm merge valley simulator in farm merge valley theme, perfectly simulating the numbers in the README.md file.

I should be able to analyze stats from previous seasons underneath the control panel and album panel when scrolling down, but this fails to appear underneath. Look for any bugs, issues or other things that need to be fixed. Perform str_replace and ensure all requirements are met.

---

# AI System Instruction: Farm Merge Valley Simulator Refinement

**Context:** You are reviewing an existing **Farm Merge Valley Simulator** (50-day season). Your task is to analyze the attached code and `README.md`, fix layout bugs, and implement/refine specific spending logic.

### 1. Objective: Logic & Probability Analysis
Analyze the full application and implement/calculate statistics for the following four sticker-purchasing scenarios. Ensure the results perfectly simulate the numbers defined in `README.md`.

Analyze the full application. Whats the result when buying stickerpacks as soon as you can afford them? What if you wait until the final moments before the season ends? What happens if you only open the best stickerpacks when you can afford them, or what happens if you spend all your stars on 5 star packs, and any remaining stars on other packs, just as the season ends? All of these scenarios need to be accounted for and easy to understand in the UI's statistics. Ensure the user gets proper statistics.

### 2. The "Vault Strategy" Overhaul
The current "Vault Strategy" section in the code/UI is confusing and requires a major logical and visual improvement. Refine this section so it clearly communicates how hoarding stars impacts the probability of collecting all stickers compared to other strategies.

### 3. Visual & UI Requirements
* **Theme:** Ensure the UI is "visually perfect" and strictly follows the **Farm Merge Valley theme**.
* **Layout Bug Fix:** There is a rendering issue where the Season Statistics fail to appear underneath the **Control Panel** and **Album Panel** when scrolling. Debug the DOM/component structure to ensure these stats are visible and correctly placed.
* **Data Clarity:** Ensure the user receives proper, easy-to-understand statistics (Completion %, average stars spent, etc.) for all scenarios within the UI.

### 4. Technical Requirements & Data Sanitization
* **Data Parsing:** Perform `str_replace` (or the language-specific equivalent) on raw data from `README.md` to ensure all numerical values are cleaned and correctly typed before being used in the simulation.
* **Code Quality:** Look for any other bugs, performance issues, or logic gaps in the current implementation and fix them.

**Goal:** A fully functional, themed, and mathematically accurate simulation that allows users to compare the efficiency of different star-spending behaviors.

---

Do a complete rework of the Vault Strategy visual design and logic. Instead of inputting the detailed strategies, simply have the five presets, and include a short description for each strategy.

End-Season Blitz should for example wait until the final day, and spend all starts on 5-star packs, if it still got remaining stars and can't afford more 5 star it buys 4 star, then 3-star.

Another mode buys 5 stars as soon as it reaches 800 stars and can afford it. Improve the entire vault strategy section with presets that explains their strategy, and make sure the currently selected option has a unique visual design so you can see which one is currently selected.

---

Do a massive visual overhaul to the event log, it needs to visually represent what's happening, instead of the current "log" display it more visually, which vault was opened? Daily login design, actual sticker designs instead of just "★1", all within a neat perfect display.

---

Update the logic when the simulation runs at "max speed", currently there is a short delay after rounds that needs to be removed from max speed, when the simulation runs at max speed, this delay needs to be removed. Also during max speed nothing visually updates in the top 100 svh, update "rounds" and "stars" so even max has some visual updates in the top 100%. (Animate even more on max speed if it has barely no impact on the simulation speed when it's being simulated at run time)